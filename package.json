{
  "name": "moodflow",
  "version": "2.0.0",
  "description": "会議中の心の変化をリアルタイムで分析するシステム",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "openai": "^4.20.0",
    "googleapis": "^128.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
